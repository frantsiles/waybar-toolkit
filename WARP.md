# Project Rules for AI Agents

- Keep changes strictly scoped to the user's request.
- Do not rely on stale list indices after reordering operations; preserve object identity for selection/state.
- Update `CHANGELOG.md` and project version metadata when preparing a release or release patch.
- Add or update `CONTRIBUTING.md` when introducing new contributor or AI workflow requirements.
- Never expose or commit secrets.
